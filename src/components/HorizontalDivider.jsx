// External imports
import React from 'react';
import Divider from '@mui/material/Divider';

// Component
const HorizontalDivider = ({ text }) => {
  return <Divider width="90%">{text}</Divider>;
};

export default HorizontalDivider;
